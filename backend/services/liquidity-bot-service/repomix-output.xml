This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  bots/
    botWorker.ts
  config/
    bots.ts
    redis.ts
  services/
    authClient.ts
    fundingClient.ts
    orderClient.ts
    priceClient.ts
    walletClient.ts
  types/
    index.ts
  index.ts
package.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/bots/botWorker.ts">
import { BotConfig, LadderLevel, Order, OrderSide } from '../types';
import { getLatestPrice } from '../services/priceClient';
import { getOpenOrders, placeOrder, cancelOrder } from '../services/orderClient';
import { getAvailableBalance } from '../services/walletClient';
import { topUpBalance } from '../services/fundingClient';

// How far (as a multiple of spreadPercent) an order's price can drift from the
// current market price before we consider it "stale" and cancel it.
const STALE_MULTIPLIER = 3;

const randomBetween = (min: number, max: number): number => {
  return min + Math.random() * (max - min);
};

const roundTo = (value: number, decimals = 8): number => {
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
};

// Builds a randomized ladder of target buy/sell levels around the mid price.
const buildTargetLadder = (config: BotConfig, midPrice: number): LadderLevel[] => {
  const levels: LadderLevel[] = [];
  const spread = config.spreadPercent / 100;

  for (let i = 0; i < config.targetOpenOrders; i++) {
    // Spread out levels a bit further from mid as i increases, with randomness
    const buyOffset = spread * randomBetween(0.3, 1) * (1 + i * 0.15);
    const sellOffset = spread * randomBetween(0.3, 1) * (1 + i * 0.15);

    levels.push({
      side: 'buy',
      price: roundTo(midPrice * (1 - buyOffset), 2),
      quantity: roundTo(randomBetween(config.minOrderQty, config.maxOrderQty), 6)
    });

    levels.push({
      side: 'sell',
      price: roundTo(midPrice * (1 + sellOffset), 2),
      quantity: roundTo(randomBetween(config.minOrderQty, config.maxOrderQty), 6)
    });
  }

  return levels;
};

// Determines which open orders have drifted too far from the current market
// price and should be cancelled to make room for fresher ones.
const findStaleOrders = (config: BotConfig, openOrders: Order[], midPrice: number): Order[] => {
  const staleThreshold = (config.spreadPercent / 100) * STALE_MULTIPLIER;

  return openOrders.filter(order => {
    const deviation = Math.abs(order.price - midPrice) / midPrice;
    return deviation > staleThreshold;
  });
};

// Ensures the bot has enough balance to place an order, topping up once if needed.
const ensureBalance = async (
  config: BotConfig,
  side: OrderSide,
  price: number,
  quantity: number
): Promise<boolean> => {
  const asset = side === 'buy' ? config.quoteAsset : config.baseAsset;
  const required = side === 'buy' ? price * quantity : quantity;
  const minThreshold = side === 'buy' ? config.minQuoteBalance : config.minBaseBalance;

  let available = await getAvailableBalance(config.id, asset);

  if (available >= required) {
    return true;
  }

  if (available < minThreshold) {
    console.log(`[BotWorker:${config.id}] Low ${asset} balance (${available}), topping up...`);
    const success = await topUpBalance(config.id, asset, config.topUpAmount);
    if (!success) {
      return false;
    }
    available = await getAvailableBalance(config.id, asset);
  }

  return available >= required;
};

// Runs a single maintenance cycle for one bot: fetch price, cancel stale
// orders, and top up to the target order count on each side.
export const runBotCycle = async (config: BotConfig): Promise<void> => {
  const priceData = await getLatestPrice(config.symbol);

  if (!priceData) {
    console.warn(`[BotWorker:${config.id}] No price available for ${config.symbol}, skipping cycle`);
    return;
  }

  const midPrice = priceData.price;
  const openOrders = await getOpenOrders(config.id, config.symbol);

  // Cancel stale orders first, so their capacity frees up for this cycle's top-up
  const staleOrders = findStaleOrders(config, openOrders, midPrice);
  for (const order of staleOrders) {
    const cancelled = await cancelOrder(config.id, order.id);
    if (cancelled) {
      console.log(`[BotWorker:${config.id}] Cancelled stale ${order.side} order ${order.id} @ ${order.price}`);
    }
  }

  const remainingOrders = openOrders.filter(o => !staleOrders.includes(o));
  const openBuyCount = remainingOrders.filter(o => o.side === 'buy').length;
  const openSellCount = remainingOrders.filter(o => o.side === 'sell').length;

  const buysNeeded = Math.max(0, config.targetOpenOrders - openBuyCount);
  const sellsNeeded = Math.max(0, config.targetOpenOrders - openSellCount);

  if (buysNeeded === 0 && sellsNeeded === 0) {
    return;
  }

  const ladder = buildTargetLadder(config, midPrice);
  const buyLevels = ladder.filter(l => l.side === 'buy').slice(0, buysNeeded);
  const sellLevels = ladder.filter(l => l.side === 'sell').slice(0, sellsNeeded);

  for (const level of [...buyLevels, ...sellLevels]) {
    const hasBalance = await ensureBalance(config, level.side, level.price, level.quantity);

    if (!hasBalance) {
      console.warn(`[BotWorker:${config.id}] Insufficient balance for ${level.side} ${level.quantity} @ ${level.price}, skipping`);
      continue;
    }

    const order = await placeOrder(config.id, {
      symbol: config.symbol,
      side: level.side,
      type: 'limit',
      price: level.price,
      quantity: level.quantity
    });

    if (order) {
      console.log(`[BotWorker:${config.id}] Placed ${level.side} ${level.quantity} @ ${level.price}`);
    }
  }
};

// Starts the recurring interval loop for one bot. Returns the interval handle
// so index.ts can clear it on shutdown if needed.
export const startBotWorker = (config: BotConfig): NodeJS.Timeout => {
  console.log(`[BotWorker:${config.id}] Starting for ${config.symbol}, target ${config.targetOpenOrders} orders/side`);

  // Run once immediately, then on the configured interval
  runBotCycle(config).catch(err => console.error(`[BotWorker:${config.id}] Cycle error:`, err.message));

  return setInterval(() => {
    runBotCycle(config).catch(err => console.error(`[BotWorker:${config.id}] Cycle error:`, err.message));
  }, config.refreshIntervalMs);
};
</file>

<file path="src/config/bots.ts">
import { BotConfig } from '../types';

// Preconfigured bot accounts. These accounts must already exist in auth-service —
// this service never signs up or creates users.
export const BOTS: BotConfig[] = [
  {
    id: 'bot-btc-1',
    email: process.env.BOT_BTC_1_EMAIL || 'bot.btc.1@nexustrade.internal',
    password: process.env.BOT_BTC_1_PASSWORD || 'changeme',
    symbol: 'BTCUSDT',
    spreadPercent: 0.4,
    minOrderQty: 0.001,
    maxOrderQty: 0.01,
    targetOpenOrders: 6,       // per side (6 buy + 6 sell = 12 total)
    refreshIntervalMs: 15000,
    quoteAsset: 'USDT',
    baseAsset: 'BTC',
    minQuoteBalance: 2000,
    minBaseBalance: 0.05,
    topUpAmount: 5000
  },
  {
    id: 'bot-eth-1',
    email: process.env.BOT_ETH_1_EMAIL || 'bot.eth.1@nexustrade.internal',
    password: process.env.BOT_ETH_1_PASSWORD || 'changeme',
    symbol: 'ETHUSDT',
    spreadPercent: 0.5,
    minOrderQty: 0.01,
    maxOrderQty: 0.2,
    targetOpenOrders: 6,
    refreshIntervalMs: 15000,
    quoteAsset: 'USDT',
    baseAsset: 'ETH',
    minQuoteBalance: 1500,
    minBaseBalance: 1,
    topUpAmount: 3000
  }
];
</file>

<file path="src/config/redis.ts">
import Redis from 'ioredis';

export const redis = new Redis({
  host: process.env.REDIS_HOST || 'localhost',
  port: Number(process.env.REDIS_PORT) || 6379
});

redis.on('connect', () => {
  console.log('Liquidity Bot connected to Redis');
});

redis.on('error', (err) => {
  console.error('Redis connection error:', err.message);
});
</file>

<file path="src/services/authClient.ts">
import axios from 'axios';
import { BOTS } from '../config/bots';
import { BotSession, LoginResponse } from '../types';

const GATEWAY_URL = process.env.GATEWAY_URL || 'http://localhost:3000';

// In-memory token cache, keyed by bot id
const sessions = new Map<string, BotSession>();

// Fallback TTL if auth-service doesn't return expiresIn (50 min, JWTs are commonly 1hr)
const DEFAULT_TTL_MS = 50 * 60 * 1000;

const login = async (botId: string): Promise<BotSession> => {
  const bot = BOTS.find(b => b.id === botId);
  if (!bot) {
    throw new Error(`Unknown bot id: ${botId}`);
  }

  const response = await axios.post<LoginResponse>(`${GATEWAY_URL}/api/auth/login`, {
    email: bot.email,
    password: bot.password
  });

  const { token, expiresIn } = response.data;
  const ttlMs = expiresIn ? expiresIn * 1000 : DEFAULT_TTL_MS;

  const session: BotSession = {
    botId,
    token,
    expiresAt: Date.now() + ttlMs
  };

  sessions.set(botId, session);
  console.log(`[auth] Bot ${botId} logged in, token valid until ${new Date(session.expiresAt).toISOString()}`);

  return session;
};

// Called once at startup for every configured bot
export const loginAllBots = async (): Promise<void> => {
  for (const bot of BOTS) {
    try {
      await login(bot.id);
    } catch (err: any) {
      console.error(`[auth] Failed to log in bot ${bot.id}:`, err.message);
    }
  }
};

// Returns a valid token for the bot, re-logging in if missing/expired.
// Call this before every API call — cheap no-op when the cached token is still fresh.
export const getToken = async (botId: string): Promise<string> => {
  const existing = sessions.get(botId);

  if (existing && existing.expiresAt > Date.now() + 5000) {
    return existing.token;
  }

  const session = await login(botId);
  return session.token;
};

// Call this if a request comes back 401 mid-flight, to force a fresh login
// even if our local expiry clock thought the token was still valid.
export const invalidateSession = (botId: string): void => {
  sessions.delete(botId);
};
</file>

<file path="src/services/fundingClient.ts">
import axios from 'axios';
import { getToken, invalidateSession } from './authClient';
import { DepositIntentResponse } from '../types';

const GATEWAY_URL = process.env.GATEWAY_URL || 'http://localhost:3000';

// Reuses the existing funding-service deposit flow: create an intent, then
// simulate the crypto deposit against that intent. No new funding logic —
// just chains the two existing endpoints.
export const topUpBalance = async (
  botId: string,
  asset: string,
  amount: number
): Promise<boolean> => {
  const token = await getToken(botId);
  const headers = { Authorization: `Bearer ${token}` };

  try {
    const intentResponse = await axios.post<DepositIntentResponse>(
      `${GATEWAY_URL}/api/funding/deposit/intent`,
      { asset, amount },
      { headers }
    );

    const { intentId } = intentResponse.data;

    await axios.post(
      `${GATEWAY_URL}/api/funding/deposit/simulate-crypto`,
      { intentId },
      { headers }
    );

    console.log(`[fundingClient] Bot ${botId} topped up ${amount} ${asset}`);
    return true;
  } catch (err: any) {
    if (err.response?.status === 401) {
      invalidateSession(botId);
    }
    console.error(`[fundingClient] Top-up failed for ${botId} (${asset}):`, err.message);
    return false;
  }
};
</file>

<file path="src/services/orderClient.ts">
import axios from 'axios';
import { getToken, invalidateSession } from './authClient';
import { Order, PlaceOrderRequest } from '../types';

const GATEWAY_URL = process.env.GATEWAY_URL || 'http://localhost:3000';

// All order operations go through the same public /api/orders/* routes
// exposed via api-gateway — identical to what the mobile app calls.
// No Redis, Kafka, or matching-engine access from this service at all.

export const placeOrder = async (
  botId: string,
  request: PlaceOrderRequest
): Promise<Order | null> => {
  const token = await getToken(botId);

  try {
    const response = await axios.post<Order>(`${GATEWAY_URL}/api/orders`, request, {
      headers: { Authorization: `Bearer ${token}` }
    });
    return response.data;
  } catch (err: any) {
    if (err.response?.status === 401) {
      invalidateSession(botId);
    }
    console.error(`[orderClient] Failed to place order for ${botId}:`, err.message);
    return null;
  }
};

export const getOpenOrders = async (botId: string, symbol: string): Promise<Order[]> => {
  const token = await getToken(botId);

  try {
    const response = await axios.get<Order[]>(`${GATEWAY_URL}/api/orders`, {
      headers: { Authorization: `Bearer ${token}` },
      params: { symbol, status: 'open' }
    });
    return response.data;
  } catch (err: any) {
    if (err.response?.status === 401) {
      invalidateSession(botId);
    }
    console.error(`[orderClient] Failed to fetch open orders for ${botId}:`, err.message);
    return [];
  }
};

export const cancelOrder = async (botId: string, orderId: string): Promise<boolean> => {
  const token = await getToken(botId);

  try {
    await axios.delete(`${GATEWAY_URL}/api/orders/${orderId}`, {
      headers: { Authorization: `Bearer ${token}` }
    });
    return true;
  } catch (err: any) {
    if (err.response?.status === 401) {
      invalidateSession(botId);
    }
    console.error(`[orderClient] Failed to cancel order ${orderId} for ${botId}:`, err.message);
    return false;
  }
};
</file>

<file path="src/services/priceClient.ts">
import { redis } from '../config/redis';
import { PriceData } from '../types';

// Reads the latest price for a symbol from Redis (written by market-data-service).
// Read-only — this service never writes to price:<SYMBOL>.
export const getLatestPrice = async (symbol: string): Promise<PriceData | null> => {
  try {
    const raw = await redis.get(`price:${symbol}`);
    if (!raw) {
      return null;
    }
    return JSON.parse(raw) as PriceData;
  } catch (err: any) {
    console.error(`[priceClient] Failed to read price for ${symbol}:`, err.message);
    return null;
  }
};
</file>

<file path="src/services/walletClient.ts">
import axios from 'axios';
import { getToken, invalidateSession } from './authClient';
import { Balance } from '../types';

const GATEWAY_URL = process.env.GATEWAY_URL || 'http://localhost:3000';

// Fetches all balances for a bot account via the existing wallet-service endpoint.
// Assumption: GET /api/wallet/balance returns an array of { asset, available, locked }
// for the authenticated user. Adjust the parsing below if your endpoint instead
// takes a ?asset= query param or returns a single object.
export const getBalances = async (botId: string): Promise<Balance[]> => {
  const token = await getToken(botId);

  try {
    const response = await axios.get<Balance[]>(`${GATEWAY_URL}/api/wallet/balance`, {
      headers: { Authorization: `Bearer ${token}` }
    });
    return response.data;
  } catch (err: any) {
    if (err.response?.status === 401) {
      invalidateSession(botId);
    }
    console.error(`[walletClient] Failed to fetch balances for ${botId}:`, err.message);
    return [];
  }
};

// Convenience helper: get the available balance for one specific asset.
export const getAvailableBalance = async (botId: string, asset: string): Promise<number> => {
  const balances = await getBalances(botId);
  const match = balances.find(b => b.asset === asset);
  return match?.available ?? 0;
};
</file>

<file path="src/types/index.ts">
// ---- Bot configuration ----

export interface BotConfig {
  id: string;              // internal identifier, e.g. 'bot-1'
  email: string;            // preconfigured account email
  password: string;         // preconfigured account password
  symbol: string;            // e.g. 'BTCUSDT' — matches Redis key price:<SYMBOL>
  spreadPercent: number;     // e.g. 0.5 => orders placed +-0.5% around mid price
  minOrderQty: number;
  maxOrderQty: number;
  targetOpenOrders: number;  // desired number of live orders per side
  refreshIntervalMs: number; // how often the bot's loop runs
  quoteAsset: string;        // e.g. 'USDT' — asset balance checked before buys
  baseAsset: string;         // e.g. 'BTC' — asset balance checked before sells
  minQuoteBalance: number;   // top-up trigger threshold for quote asset
  minBaseBalance: number;    // top-up trigger threshold for base asset
  topUpAmount: number;       // amount to request when topping up
}

// ---- Market data (mirrors market-data-service's Redis payload) ----

export interface PriceData {
  asset: string;
  price: number;
  amount: number;
  timestamp: string;
  source: string;
}

// ---- Auth ----

export interface LoginResponse {
  token: string;
  expiresIn?: number; // seconds, if provided by auth-service; otherwise we assume a default TTL
}

export interface BotSession {
  botId: string;
  token: string;
  expiresAt: number; // epoch ms
}

// ---- Wallet ----

export interface Balance {
  asset: string;
  available: number;
  locked?: number;
}

// ---- Funding ----

export interface DepositIntentResponse {
  intentId: string;
  asset: string;
  amount: number;
}

export interface SimulateCryptoDepositRequest {
  intentId: string;
}

// ---- Orders ----

export type OrderSide = 'buy' | 'sell';
export type OrderStatus = 'open' | 'filled' | 'cancelled' | 'partial';

export interface Order {
  id: string;
  symbol: string;
  side: OrderSide;
  price: number;
  quantity: number;
  status: OrderStatus;
  createdAt: string;
}

export interface PlaceOrderRequest {
  symbol: string;
  side: OrderSide;
  type: 'limit';
  price: number;
  quantity: number;
}

// ---- Ladder planning (used internally by BotWorker) ----

export interface LadderLevel {
  side: OrderSide;
  price: number;
  quantity: number;
}
</file>

<file path="src/index.ts">
import express from 'express';
import { BOTS } from './config/bots';
import { loginAllBots } from './services/authClient';
import { startBotWorker } from './bots/BotWorker';

const app = express();
const PORT = process.env.PORT || 3006;

const activeWorkers = new Map<string, NodeJS.Timeout>();

app.get('/health', (_req, res) => {
  res.json({
    status: 'ok',
    bots: BOTS.map(b => ({
      id: b.id,
      symbol: b.symbol,
      running: activeWorkers.has(b.id)
    }))
  });
});

const bootstrap = async () => {
  console.log(`Liquidity Bot Service starting with ${BOTS.length} bot(s)...`);

  await loginAllBots();

  for (const bot of BOTS) {
    const handle = startBotWorker(bot);
    activeWorkers.set(bot.id, handle);
  }

  app.listen(PORT, () => {
    console.log(`Liquidity Bot Service health endpoint running on port ${PORT}`);
  });
};

bootstrap().catch(err => {
  console.error('Failed to bootstrap Liquidity Bot Service:', err);
  process.exit(1);
});

process.on('SIGTERM', () => {
  console.log('Shutting down bot workers...');
  for (const handle of activeWorkers.values()) {
    clearInterval(handle);
  }
  process.exit(0);
});
</file>

<file path="package.json">
{
  "name": "Liquidity-bot-service",
  "version": "1.0.0",
  "description": "Liquidity bot that maintains realistic buy/sell orders around market price via the Order Service API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^5.0.6",
    "axios": "^1.7.9",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
</file>

</files>
